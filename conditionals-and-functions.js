// Conditionals, Functions, Scope, and Loops
let equals = 1 === 1;
let greaterThan = 5 > 2;
let lessThan = 2 < 10;
let greaterThanEq = 5 >= 5;
let lessThanEq = 3 <= 7;
let notEquals = 3 !== 2

// === vs == is strict vs. non strict equality. == will convert types, === will check that types are equal.
// same goes for != vs !== - the extra = means that it's checking type, too.
// 1 == "1" is true, but 1 === "1" is false because one is a number and the other a string.

let storeA = 4.10;
let storeB = 4.10;

let storeAisLower = storeA < storeB;
console.log(storeAisLower);

if (storeAisLower) {
    console.log("Store A has a lower price.")
} else if (storeB < storeA) {
    console.log("Store B has a lower price.")
} else {
    console.log("Both stores' prices are equal.")
}

function compareStorePrices (storeA, storeB) {
    let storeAisLower = storeA < storeB;
    if (storeAisLower) {
        console.log("Store A has a lower price.")
    } else if (storeB < storeA) {
        console.log("Store B has a lower price.")
    } else {
        console.log("Both stores' prices are equal.")
    }
}

compareStorePrices(7,5);
compareStorePrices(5,10);

function squareNum(number) {
    return number * number;
}
let squaredNumber = squareNum(5);
console.log(squaredNumber);
// we're returning the variable squaredNumber, which is generated by the function. We can't return the function itself.

//SCOPE: Basically, if the variable is declared inside the function, it only exists in the function and can't be fetched outside of it.
//variables defined outside of the function CAN be accessed inside the function.
//using the same variable inside the function will override global scope

let x = 10;
function addNumbers (n, m) {
    console.log(x);
    return n + m;
}
addNumbers(2,3);
// in this case the "addNumbers" function isn't adding any numbers, it's only running its 2nd line and loggin Global x variable
// If I add x as a third variable to the function, it will override the global x variable.

function addNumbers2 (n, m, newNum) {
    console.log(newNum); //not defined WITHIN the function, so "undefined" is returned until you define it when calling the function:
    return n + m;
}
addNumbers2(2,3,21);
console.log(x);
// this one is pulling from the global again, because it's outside the function.

//JS has 3 levels of scope - global, function, and block.

function addNumbers3 (n, m, x) {
    console.log(n + m + x);
    let b;
    if (1===1) {
        b = 8;
    }
    console.log(b);
}
addNumbers3(2,3,4); //this wasn't working becasue you just made the function, you didn't execute it until adding this line.

//ARRAYS (a type of data structure):

let ourArray = [1,2,3,4,[5,1],6,7,8,9,10];
console.log(ourArray[0]);
console.log(ourArray[4][0]);

//LOOPS:
//for looks have 3 elements, counter, comparison, and incrementer.
//below: counter (i starts at 0), comparison (keep looping while i<5, incrementer (add 1 to i on each loop)
for (let i = 0; i<5; i++) {
    //console.log("i is equal to: " + i)
    console.log(ourArray[i]);
}
// To capture the whole length of the array and be safe to future edits to the array, use i<ourArray.length or make a new variable for array length

for (let i=0; i<ourArray.length; i++) {
    console.log(ourArray[i]);
}

let arrLen = ourArray.length;
for (let i=0; i<arrLen; i++) {
    console.log(ourArray[i]);
    //for (let j=0; j<arrLen; j++) {.  <-- a loop within a loop will run fully on every loop (careful!)
    //    console.log(ourArray[j])
    }

//WHILE LOOPS just run while a condition is true, until it is false
let p = 0;
while (p < 5) {
    console.log("Ran");
    p = p + 1;
}